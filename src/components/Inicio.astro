---
import { getCollection } from "astro:content";

const books = await getCollection("books");

export const prerender = true;
---

<main>
  <h1 class="text-center text-7xl font-extrabold mb-24">
    Devs
    <span class="block">Books</span>
  </h1>
  <section aria-label="libro recomendados para programadores">
    <div class="px-4 mx-auto max-w-screen-xl">
      <div class="grid gap-6 md:grid-cols-2">
        {
          books.map((book) => {
            const { slug, data } = book;
            const { title, author, img, readtime, description, buy } = data;
			
            return (
              <article class="flex flex-col md:flex-row">
                <a href={`/libro/${slug}`} class="mb-2 xl:mb-0 transition hover:scale-110">
                  <img 
                    transition:name={`img-${slug}`}
                    class="rounded-lg md:mr-5 w-full md:w-48 md:min-w-48"
                    src={`/${img}`} 
                    alt={title} />
                </a>

                <div class="flex flex-col justify-center">
                  <h2 class="mb-2 text-2xl font-bold leading-tight text-white">
                    {title}
                  </h2>
                  <p class="mb-4 text-gray-300 max-w-sm"> {description} </p>
                  <span class="text-xs">
                    ⏳ Tiempo de lectura en {readtime} minutos
                  </span>
                </div>
              </article>
            );
          })
        }
      </div>
    </div>
  </section>
</main>
